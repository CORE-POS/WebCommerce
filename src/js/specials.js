jQuery(document).ready(function() {
    /*
    jQuery('.catalog-header h3').each(function() {
        var sectionID = jQuery(this).attr('id');
        var sectionName = jQuery(this).html();
        var elem = '<li><a href="#' + sectionID + '" class="page-anchor">';
        elem += '<em>' + sectionName + '</em></a></li>';
        jQuery('ul#specials-section-list').append(elem);
    });
    */
});
